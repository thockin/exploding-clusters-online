DEVMODE timer is 2s
 - use "Put a card back" button in tests instead of playing
 - revert to 8
Comment test cases
Const for "Give me a ..." button in tests
Merge timer test with play card / play combo tests
Think about nonce in reaction loop
Log popped cards (change ops from func to {Card, func})
Clean up server and client logs
DEVMODE - card counts overlap the cards

`npm i baseline-browser-mapping@latest -D` error, but we are at latest

Later:
* Hand animation is ugly
  - need configurable skin colors?
* Scale cards up in size as they get closer to discard pile (drag)
* Visual glitch when playing a single developer card (should be unplayable)
* Reduce card spacing in hand or even overlap them?
* copyrights on art and code
