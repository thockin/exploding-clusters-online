Phase 5:
* s/duration/timeout for server-side implemented timeouts
* get rid of "show" or "active" on most modals?
* rename game timer/timerDuration - is it only reactionTimer?

tests
 - more helpers
 - better locator for all areas
 - remove unused locator consts
 - Add a prod test with cases which do not need DEVMODE
 - Add tests for card choice auto-timeout


`npm i baseline-browser-mapping@latest -D` error, but we are at latest

Later:
* Change mouse cursor when deck is not clickable (not your turn)
* document state-machine
  * Rename TurnPhase to TurnState ?
* Play-card animation (important when playing the same card)
* Re-insert card animation
* Hand animation is ugly
  - need configurable skin colors?
  - scale hand animations with cards
  - center on deck
* Shuffle animation
* Scale cards up in size as they get closer to discard pile (drag)
* Announce who is the first player
* Announce the victim more visibly when FAVOR or combo is played
* Reduce card spacing in hand or even overlap them?
* copyrights on art and code
* Capitalization and grammar/style in all buttons and labels
 - Modal dialog for "beat you to it"
* colors for reaction timer text
* Send card classes to logs and messages as `s/_/ /g`
* Nonce updates in `finally` blocks (redundant updates?)
* Better messages for NAK (e.g. "P2 NAK'ed the SHUFFLE")
* Fix dumb LLM comments like "Maybe we should" and "design doc says..." and "phase X"
 - send BIG messages when executing
   - Eg "Tim's NAK cancels Bob's SHUFFLE" and "Shuffling the deck" as we pop ops

Bugs:
* if non-current player plays NAK out of turn (8 cards->7), they get a
  handUpdate to revert (to 8), but sometimes set hand AGAIN to 7. Hard to
  repro, no other event received?
